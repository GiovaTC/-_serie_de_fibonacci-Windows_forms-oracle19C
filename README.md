# -_serie_de_fibonacci-Windows_forms-oracle19C:.
# 🧮🔢 Serie de Fibonacci + Windows Forms + Oracle 19c .

███████╗██╗██████╗ ██████╗  █████╗ ███╗  ██╗ █████╗ ██████╗ ██╗
██╔════╝██║██╔══██╗██╔══██╗██╔══██╗████╗ ██║██╔══██╗██╔══██╗██║
█████╗  ██║██████╦╝██████╔╝███████║██╔██╗██║███████║██████╔╝██║
██╔══╝  ██║██╔══██╗██╔══██╗██╔══██║██║╚████║██╔══██║██╔═══╝ ██║
██║     ██║██████╦╝██║  ██║██║  ██║██║ ╚███║██║  ██║██║     ██║
╚═╝     ╚═╝╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚══╝╚═╝  ╚═╝╚═╝     ╚═╝

    🧮 Serie de Fibonacci | 🖥 .NET C# | 🗄 Oracle 19c

## 📌 Funcionalidades:

✔ Genera los primeros N números de la serie de Fibonacci  
✔ Muestra el resultado en un TextBox (multilínea)  
✔ Al presionar Guardar, inserta los datos en la tabla Oracle  
✔ Incluye manejo básico de errores

## 📂 1. Estructura de la tabla Oracle:

```sql
CREATE TABLE SERIE_FIBONACCI (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NVALOR NUMBER NOT NULL,
    SERIE VARCHAR2(4000),
    FECHA_REGISTRO DATE DEFAULT SYSDATE
);
```

## 🖥️ 2. Diseño del Formulario (Windows Forms):

| Control| Nombre      | Uso                            |
|--------|-------------|--------------------------------|
| Label  | lblN        | Texto "Ingrese N:"             |
| TextBox| txtN        | Valor de N                     |
| Button | btnGenerar  | Generar serie                  |
| TextBox (Multiline)  | txtResultado | Mostrar serie   |
| Button | btnGuardar  | Insertar en Oracle             |
| Label  | lblEstado   | Mensajes de éxito/error        |

## 🧑‍💻 3. Código completo Form1.cs:

```csharp
using System;
using System.Collections.Generic;
using System.Data;
using System.Windows.Forms;
using Oracle.ManagedDataAccess.Client; // NuGet: Oracle.ManagedDataAccess

namespace FibonacciApp
{
    public partial class Form1 : Form
    {
        private string connectionString = "User Id=USUARIO;Password=PASSWORD;Data Source=localhost:1521/XEPDB1;";

        public Form1()
        {
            InitializeComponent();
        }

        private List<int> GenerarFibonacci(int n)
        {
            List<int> serie = new List<int>();
            int a = 0, b = 1;

            for (int i = 0; i < n; i++)
            {
                serie.Add(a);
                int temp = a + b;
                a = b;
                b = temp;
            }
            return serie;
        }

        private void btnGenerar_Click(object sender, EventArgs e)
        {
            try
            {
                int n = int.Parse(txtN.Text);
                var serie = GenerarFibonacci(n);
                txtResultado.Text = string.Join(", ", serie);
                lblEstado.Text = "Serie generada correctamente.";
            }
            catch
            {
                lblEstado.Text = "Error: ingrese un número válido.";
            }
        }

        private void btnGuardar_Click(object sender, EventArgs e)
        {
            if (string.IsNullOrWhiteSpace(txtResultado.Text))
            {
                lblEstado.Text = "Primero genere la serie.";
                return;
            }

            try
            {
                using (OracleConnection conn = new OracleConnection(connectionString))
                {
                    conn.Open();
                    string query = "INSERT INTO SERIE_FIBONACCI (NVALOR, SERIE) VALUES (:n, :serie)";
                    using (OracleCommand cmd = new OracleCommand(query, conn))
                    {
                        cmd.Parameters.Add(":n", int.Parse(txtN.Text));
                        cmd.Parameters.Add(":serie", txtResultado.Text);
                        cmd.ExecuteNonQuery();
                        lblEstado.Text = "Datos registrados en Oracle.";
                    }
                }
            }
            catch (Exception ex)
            {
                lblEstado.Text = "Error BD: " + ex.Message;
            }
        }
    }
}
```

## ⚙️ 4. Dependencias:

```bash
Install-Package Oracle.ManagedDataAccess
```

## 🧪 5. Prueba rápida:

1. Ejecuta el programa  
2. Ingresa un número (ej: 10)  
3. Clic en Generar → aparece la serie  
4. Clic en Guardar → se registra en Oracle  

## 🚀 Sugerencias

✔ Validar que N sea positivo  
✔ Añadir un DataGridView para ver registros históricos  
✔ Usar async/await para no bloquear la interfaz  
✔ Implementar logs

## 📌 Logo (ASCII)

```
 _______ _ _      _           _ _      
|__   __(_) |    | |         (_) |     
   | |   _| | ___| |__  _ __  _| | ___ 
   | |  | | |/ _ \ '_ \| '_ \| | |/ _    | |  | | |  __/ |_) | |_) | | |  __/
   |_|  |_|_|\___|_.__/| .__/|_|_|\___|
                       | |             
                       |_|    Fibonacci + Oracle
``` :. .
